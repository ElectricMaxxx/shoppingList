var config = require("./config.json");
//var navigation_view = require();
var jade = require("jade");
 function navigation(req,res)
{
    this.req = req;
    this.res = res;
    return function()
    {
        return config.navigation;
    };
    
};
module.exports = navigation;